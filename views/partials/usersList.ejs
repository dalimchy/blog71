                <div class="card">
                            <div class="card-header">
                                <h4>All Users</h4>
                            </div>
                            <div class="card-block">
                                <!-- DataTables init on table by adding .js-dataTable-full class, functionality initialized in js/pages/base_tables_datatables.js -->
                                <table class="table table-bordered table-striped table-vcenter js-dataTable-full">
                                    <thead>
                                        <tr>
                                            <th class="text-center"></th>
                                            <th>Name</th>
                                            <th class="hidden-xs">Email</th>
                                            <th class="hidden-xs w-20">Phone</th>
                                            <th class="hidden-xs w-10">Role</th>
                                            <th class="text-center" style="width: 10%;">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if(title == 'Dashboard'){%>
                                            <% if(data[0].users != undefined){%>
                                                <% var i = 1; %>
                                                <% _.each(data[0].users, function(v,k){%>
                                                    <% if(v._id != user_id){%>
                                                        <tr id="userId_<%= v._id%>" data-id="<%= v._id %>">
                                                            <td class="text-center"><%= i++ %></td>
                                                            <td class="font-500"><%= v.name %></td>
                                                            <td class="hidden-xs"><%= v.email%></td>
                                                            <td class="hidden-xs"><%= v.phone%></td>
                                                            <td class="hidden-xs"><%= v.type%></td>
                                                            <td class="text-center">
                                                                <div class="btn-group">
                                                                    <button id="userView_<%= v._id%>" data-id="<%= v._id%>" class="btn btn-xs btn-default" type="button" data-toggle="tooltip" title="" data-original-title="View User"><i class="fa fa-tripadvisor"></i></button>
                                                                    <% if(user_type == 'admin'){%>
                                                                        <% if(v.type != 'admin'){%>
                                                                            <button id="userEdit_<%= v._id%>" data-id="<%= v._id%>" class="btn btn-xs btn-default" type="button" data-toggle="tooltip" title="" data-original-title="Edit User"><i class="ion-edit"></i></button>
                                                                            <button id="userDelete_<%= v._id%>" data-id="<%= v._id%>" class="btn btn-xs btn-default" type="button" data-toggle="tooltip" title="" data-original-title="Remove User"><i class="ion-close"></i></button>
                                                                        <%}%>
                                                                    <%}%>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    <%}%>
                                                <%})%>
                                            <%}%>
                                        <%}%>
                                    </tbody>
                                </table>
                            </div>
                            <!-- .card-block -->
                </div>